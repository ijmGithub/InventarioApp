<UserControl x:Class="InventarioApp.Views.ProductosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel Orientation="Vertical">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <TextBlock Text="Categoria:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox ItemsSource="{Binding Categorias}"
                          SelectedItem="{Binding CategoriaSeleccionada}"
                          DisplayMemberPath="nombre"
                          Width="150" Margin="5"/>

                <TextBlock Text="Proveedor:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox ItemsSource="{Binding Proveedores}"
                          SelectedItem="{Binding ProveedorSeleccionado}"
                          DisplayMemberPath="nombre"
                          Width="150" Margin="5"/>
            </StackPanel>

            <!-- DataGrid para mostrar los productos -->
            <DataGrid ItemsSource="{Binding Productos}" 
                      SelectedItem="{Binding ProductoSeleccionado}" 
                      AutoGenerateColumns="False"
                      Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding id}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding nombre}" />
                    <DataGridTextColumn Header="Precio" Binding="{Binding precio, StringFormat=C}" />
                    <DataGridTextColumn Header="Cantidad" Binding="{Binding cantidad}" />
                    <DataGridTextColumn Header="Categoria" Binding="{Binding Categoria.nombre}" />
                    <DataGridTextColumn Header="Proveedor" Binding="{Binding Proveedor.nombre}" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- Botones CRUD -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="Agregar" Command="{Binding AgregarProductoCommand}" Margin="5"/>
                <Button Content="Actualizar" Command="{Binding ActualizarProductoCommand}" Margin="5"/>
                <Button Content="Eliminar" Command="{Binding EliminarProductoCommand}" Margin="5"/>
                <Button Content="Guardar Cambios" Command="{Binding GuardarCambiosCommand}" Margin="5"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
